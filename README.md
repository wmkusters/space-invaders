## Space Invaders
A partially working Space Invaders game that runs via TUI using [Bubbletea](https://github.com/charmbracelet/bubbletea).

To run, make sure you have Go 1.26.2, run `go mod tidy` in the project directory to make sure you have all dependencies, then run `go run main.go`.
